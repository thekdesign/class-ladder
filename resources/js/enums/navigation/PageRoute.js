import Enum from 'enum';

export const PageRoute = new Enum([
    'HOME_INDEX',
    'QUIZ_INDEX',
    'RESULT_INDEX',
    'COMPARE_INDEX',
    'PERSONAS_INDEX',
    'DATA_INDEX',
    'NOT_FOUND',
], {ignoreCase: false});
